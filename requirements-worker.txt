-r requirements.txt

# Document processing. Only the worker pools need these - together with the
# tesseract and poppler system packages they account for most of the worker
# image, which is why the API is built from a separate target.
pypdf>=5.1.0
pytesseract>=0.3.13
pdf2image>=1.17.0
pillow>=11.0.0
# ONNX on CPU - no torch, so this stays in the hundreds of MB.
fastembed>=0.4.0
