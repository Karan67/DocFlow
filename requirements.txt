# Shared runtime: everything the API, worker, beat and flower all need.
# Document-processing dependencies live in requirements-worker.txt so the API
# image does not carry tesseract, poppler or a 130MB embedding model.

# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Persistence ---
SQLAlchemy>=2.0.36
psycopg2-binary>=2.9.10
alembic>=1.14.0
# The Job model's sibling table uses a vector column, so both halves import it.
pgvector>=0.3.6

# --- Queue ---
celery[redis]>=5.4.0
redis>=5.2.0
flower>=2.0.1

# --- Rate limiting (Redis-backed, shared across API replicas) ---
slowapi>=0.1.9

# --- Flower login gate (flowerauth) ---
# httpx does the reverse-proxying; itsdangerous signs the session cookie that
# replaces Flower's browser-native basic-auth dialog.
httpx>=0.27.0
itsdangerous>=2.2.0

# --- Object storage ---
boto3>=1.35.0
