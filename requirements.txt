# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Persistence ---
SQLAlchemy>=2.0.36
psycopg2-binary>=2.9.10
alembic>=1.14.0

# --- Queue ---
celery[redis]>=5.4.0
redis>=5.2.0
flower>=2.0.1

# --- Processing ---
pypdf>=5.1.0
# OCR for scanned PDFs with no text layer
pytesseract>=0.3.13
pdf2image>=1.17.0
pillow>=11.0.0
# Embeddings. fastembed runs ONNX on CPU - no torch, so the image stays small.
fastembed>=0.4.0
pgvector>=0.3.6
